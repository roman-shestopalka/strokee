import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // main app color
  static const mainColor = Color(0xff518E7E);

  // point colors
  static const ornPointColor = Color(0xffFF842C);
  static const blPointColor = Color(0xff2C67FF);
  static const hrPointColor = Color(0xff49FF2C);
}
